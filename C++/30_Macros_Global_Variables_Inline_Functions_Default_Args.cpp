#include <bits/stdc++.h>
using namespace std;
#define pi 3.14
// create a macro (this is not a variable: this is replaced where we write pi before the compiler)

int main()
{

    int r = 5;
    double area = pi * r * r;
    cout << "Area is : " << area << endl;

    return 0;
}